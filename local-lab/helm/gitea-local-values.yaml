dbType: "postgres"
useInPodPostgres: true

service:
  http:
    serviceType: NodePort
    nodePort: 32501
  ssh:
    serviceType: NodePort
    nodePort: 32502

persistence:
  enabled: true
  existingGiteaClaim: gitea-local-pv-claim

resources:
  gitea:
    requests:
      cpu: "50m"